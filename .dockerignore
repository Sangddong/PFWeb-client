# Node_modules directory (not needed in the Docker image; dependencies will be installed in the container)
node_modules

# Next.js build output (we will build this inside the Docker container)
.next

# Logs and other temporary files
npm-debug.log
yarn-debug.log
yarn-error.log
*.log

# Git files (not needed in the Docker image)
.git
.gitignore

# Dockerfile and Docker-related files
Dockerfile
.dockerignore

# Local environment variables file
.env.local
.env.development
.env.test
.env.production

# IDE/editor-specific files (usually not needed in the Docker image)
.vscode
.idea
*.swp

# Operating system files
.DS_Store
